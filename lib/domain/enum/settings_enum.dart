enum PlanType { free, premium }

enum CountryType { brasil, estadosunidos }

enum LanguageType { portugues, english }

enum TurnPhoneMode { gyroscope, countdown }

enum GameModeType { fullTrack, preview }
